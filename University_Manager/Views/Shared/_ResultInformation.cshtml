@model List<University_Manager.Models.EnrollCourse>

<script src="~/Assets/jquery/jquery.min.js"></script>
<script src="//code.jquery.com/ui/1.11.4/jquery-ui.js"></script>


<link href="~/Css/jquery.dataTables.min.css" rel="stylesheet" />
<script type="text/javascript"
        src="http://cdn.datatables.net/1.10.2/js/jquery.dataTables.min.js"></script>


@if (Model != null)
{
    <div class="panel panel-primary">
        <div class="panel-heading clearfix">
            <span class="glyphicon glyphicon-th"></span><h4 class="title">DEPARMENTS</h4>
        </div>
        <div class="container-outer" style="padding:10px">
            <table id="myTable" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>
                            Course Code
                        </th>
                        <th>
                            Course Name
                        </th>
                        <th>
                            Grade
                        </th>
                    </tr>
                <thead>
                    @foreach (var item in Model)
                {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Course.Code)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Course.Name)
                            </td>
                            <td>
                                @if (item.GradeName != null)
                    {
                                    @Html.DisplayFor(modelItem => item.GradeName)
                                }
                                @if (item.GradeName == null)
                    {
                        item.GradeName = "Empty";
                                    @Html.DisplayFor(modelItem => item.GradeName)
                                }

                            </td>
                        </tr>
                    }
            </table>
        </div>
        </div>
        }

        <div style="color: darkgoldenrod"><h3>@ViewBag.NotEnrolled</h3></div>

        <!-- jQuery -->
        <script src="~/Assets/bootstrap/js/bootstrap.min.js"></script>



        <script type="text/javascript">

            $(document).ready(function () {
                var table = $('#myTable').DataTable();

            });
        </script>
